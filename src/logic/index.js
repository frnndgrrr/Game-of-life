function countNeighbours(grid, x, y) {}

function checkNeighbours(grid) {
  return grid.map((row) => {
    return row.map((col) => {});
  });
}

export function runNewGeneration(grid) {
  console.log("Running iterative algorithm");
  return grid;
}
